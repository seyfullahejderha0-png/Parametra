import 'package:flutter/material.dart';

class AppColors {
  // Huzur verici - Deep Sea / Slate Palette
  static const Color background = Color(0xFF1E293B); // Koyu ama yumuşak bir lacivert-gri
  static const Color surface = Color(0xFF334155);    // Kartlar için daha açık bir ton
  static const Color primary = Color(0xFF38BDF8);    // Gökyüzü mavisi
  static const Color secondary = Color(0xFF818CF8);  // Yumuşak indigo
  static const Color error = Color(0xFFFB7185);
  
  static const Color textPrimary = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFF94A3B8);
  
  // Canlı ama yumuşatılmış modül renkleri
  static const Color financeColor = Color(0xFF34D399); // Zümrüt
  static const Color debtColor = Color(0xFFF87171);    // Soft Kırmızı
  static const Color diaryColor = Color(0xFF60A5FA);   // Soft Mavi
  static const Color smokingColor = Color(0xFFFB923C); // Soft Turuncu
  static const Color goalsColor = Color(0xFFA78BFA);   // Soft Mor
  static const Color aiColor = Color(0xFF22D3EE);      // Turkuaz
}
